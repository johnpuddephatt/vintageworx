---
layout: layouts/page.njk
title: Events
header:
  title: Our event programme
  description: |
    Every year we put together a programme of big events that bring people together in Falinge park.

    There’s a real mix of stuff lined up, but our main aim is to help people come together, feel good and have fun!
  image: /assets/images/event-header.jpg
---
<section class="section section--page-events">
  <div class="container">
    {% for event in collections.events %}
      <div class="row event">
        <div class="event--image">
          <img src="{{ event.data.image }}">
        </div>
        <div class="event--text">
          <h3 class="event--title">{{ event.data.title }}</h3>
          <div class="event--date">{{ event.data.date | date('dddd Do MMMM') }} {{ event.data.time }}</div>
          <p class="event--description">
            {{ event.templateContent | safe }}
          </p>
        </div>
      </div>
    {% endfor %}
  </div>
</section>
